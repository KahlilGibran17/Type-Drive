using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using TMPro;
using System.IO;
using UnityEngine.UI;
public class SparePartOil : MonoBehaviour
{
    // Start is called before the first frame update
    public int price; 
    void Start()
    {
        price = 50;

        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
